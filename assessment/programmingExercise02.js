/**
 * PROGRAMMING EXERCISE 02:
 *
 * Negate and then simplify the following statement: `!isCat && !isDog`
 * You can assume that `isCat` and `isDog` are always boolean values.
 * Assign the variable `solution` with your answer.
 *
 * Feel free to write down the steps you are taking to simplify the statement.
 */

// AS I UNDERSYAND , I USE THE NEGATION OF CONJUCTION
const solution = !isCat || !isDog;
